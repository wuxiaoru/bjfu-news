<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bjfu.news.dao.NewsEditContributionMapper">
    <insert id="insert" parameterType="com.bjfu.news.entity.NewsEditContribution">
    insert into news_edit_contribution (ID, EDITOR_ID, CONTRIBUTION_ID, 
      OPRATION, DISABLED, CREATE_TIME, 
      UPDATE_TIME, SUGGESTION)
    values (#{id,jdbcType=BIGINT}, #{editorId,jdbcType=VARCHAR}, #{contributionId,jdbcType=BIGINT}, 
      #{opration,jdbcType=INTEGER}, #{disabled,jdbcType=BIT}, #{createTime,jdbcType=TIMESTAMP}, 
      #{updateTime,jdbcType=TIMESTAMP}, #{suggestion,jdbcType=LONGVARCHAR})
  </insert>
    <select id="selectAll" resultType="com.bjfu.news.entity.NewsEditContribution">
    select ID, EDITOR_ID, CONTRIBUTION_ID, OPRATION, DISABLED, CREATE_TIME, UPDATE_TIME, 
    SUGGESTION
    from news_edit_contribution
  </select>

    <select id="selectById" resultType="com.bjfu.news.entity.NewsEditContribution">
       select * from news_edit_contribution where  DISABLED=false  and ID=#{id}
   </select>

    <select id="selectByCId" resultType="com.bjfu.news.entity.NewsEditContribution">
        select * from news_edit_contribution  where disabled = false and contributionId=#{contributionId}
   </select>

    <select id="selectByEditorId" resultType="com.bjfu.news.entity.NewsEditContribution">
        select * from news_edit_contribution  where disabled = false and editorId=#{editorId}
   </select>

    <select id="list" parameterType="com.bjfu.news.req.ContributionReq"
            resultType="com.bjfu.news.entity.NewsEditContribution">
        select * from news_edit_contribution where disabled = false and editorId=#{userId}
        <if test="contributionIds!=null">
            and contributionId in
            <foreach item="contributionId" index="index" collection="contributionIds">
                #{contributionId}
            </foreach>
        </if>
        litmit #{start},#{size}
    </select>

    <select id="getCount" parameterType="com.bjfu.news.req.ContributionReq"
            resultType="Integer">
        select count(*) from news_edit_contribution where disabled = false and editorId=#{userId}
        <if test="contributionIds!=null">
            and contributionId in
            <foreach item="contributionId" index="index" collection="contributionIds">
                #{contributionId}
            </foreach>
        </if>
    </select>

    <update id="delete" parameterType="com.bjfu.news.req.IdsParam">
        update news_edit_contribution set disabled = true where id in
        <foreach item="id" index="index" collection="ids">
            #{id}
        </foreach>
    </update>

    <update id="updateOperate" parameterType="com.bjfu.news.entity.NewsEditContribution">
        update news_edit_contribution set
        opration=#{opration},
        suggestion=#{suggestion}
        where ID=#{id}
    </update>
</mapper>